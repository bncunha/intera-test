import styled from 'styled-components';
import colors from '../../styles/colors';

export const Label = styled.label`
  font-weight 600;
  color: ${colors.title};
  display: block;
  margin-bottom: 5px;
`;
